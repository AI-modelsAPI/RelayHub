# RelayHub 修复状态报告 — 2026-09-22

- **分支**：`arena/01a0c14d-relayhub`（修复全部落在本分支，随后合并 main 并删除本分支）
- **基线**：`main @ c4c332c`
- **依据输入**（两份报告均保持原样，不改一字）：
  - **报告 A**（本工作流产出）：`docs/audit/AUDIT-2026-09-22.md`（P0×2 / P1×11 / P2×12 / P3×10）
  - **报告 B**（分支 `arena/01a0c143-relayhub` 的 `AUDIT-REPORT.md`，已取回参考；其探针脚本 `scripts/audit-evidence.py` 已检入本仓库 `scripts/`）：RH-01～RH-33
- **验证环境限制**：本沙箱无 Go 工具链且无法联网安装；
  `go build/vet/test -race` 未能在本机执行。已执行：`gofmt -l`（干净）、全量静态复读、`node` 契约测试未发现回归、以及逐项锚点比对。**合并后必须补跑 `go test -race ./...`。**

## 一、总体完成度

| 报告 | 覆盖 | 说明 |
| --- | --- | --- |
| A（35 项） | 30 项已修复/缓解，5 项设计留档（见第三节） | P0 ×2 全部修复 |
| B（33 项） | 26 项已修复/缓解，7 项留档 | P1 全部处置 |

## 二、逐项修复对照

### 2.1 网关与路由器核心

| 项 | 处置 | 位置 |
| --- | --- | --- |
| A-P0-2 / RH-07 负载均衡退化 | `Request.Source` 在网关入口注入（全局原子计数）；`NewCryptoSource()` 提供随机策略真随机源；`Rand` 已在生产接线 | `internal/gateway/gateway.go`、`internal/router/selector.go`、`internal/app/wiring.go` |
| RH-08 / A-P1-4 resolver 首次发布竞态 | 加 `statePublish sync.RWMutex`，值接收者语义的注释与一次发布保护 | `internal/router/selector.go` |
| RH-02 / A-P1-3 模型组回环 + 优先级过滤 | selector 顶层 `visited` 环检测；同优先级 `cut` 修复；API 写路径`groupFallbackCycle` 预先拒绝自环/断链 | `internal/router/selector.go`、`internal/api/server.go` |
| RH-02 ProviderModel 跨 Provider/化绑定 | 管理 API 写时校验 `channel_id` 归属 + 存在性 | `internal/api/server.go` |
| RH-06 / A-P1-9 Runaway Guard 未接入 | Guard 在 `ServeHTTP` 热路径 `Trip`；请求无 SessionKey 时跳过 | `internal/gateway/gateway.go`、`internal/guard/runaway.go`；wiring 已传 `Guard` |
| RH-09 CustomHeaders 不落库 | 迁移 009 新增 `channels.custom_headers` 并回填 CustomHeaders 字段；所有列表/读取 SELECT 带此列 | `internal/storage/migrations/009_*.sql`、`internal/repository/repository.go` |
| RH-11 客户端伪 `X-Forwarded-For` | 上游请求头剔除 `X-Forwarded-For/X-Real-Ip/Cf-Connecting-Ip/X-Forwarded-Host/Forwarded` | `internal/gateway/gateway.go` |
| RH-12 / A-P1-? 跨协议语义丢失 | embeddings 拒绝到 non-openai-chat 上游；transform 兜底 record 判读与 usage 回退双协议补齐 | `internal/gateway/gateway.go`、`internal/gateway/transform.go`、`internal/gateway/stream.go` |
| RH-16 ? gate 延后（签到临界滚动）| 网关路径元数据在 stream 成功/失败两处补记；checkin 并行化见 §2.5 | `internal/gateway/gateway.go` 等 |

### 2.2 渠道身份、代理与凭据（出口固定）

| 项 | 处置 | 位置 |
| --- | --- | --- |
| RH-10 / A-P1-5 渠道代理被绕过 | 新增 fail-closed 的 `identity.HTTPClientE`（非法代理 URL 直接报错，不再静默直连）；签到适配器 `CallWithAuth` 走 `clientForChannel` 并注入渠道身份头；`refresh` 在锁内重取已轮转凭据；`fetchUpstreamModelList`/`fetchModels`/`channelTest`/`channelSyncModels` 全部改走渠道代理 + 身份头 | `internal/identity/bundle.go`、`internal/adapter/base.go`、`internal/api/server.go` |
| RH-10 浏览器签到不走代理 | `CheckinRequest.ProxyURL` 透传 → `--proxy-server=` Chrome 启动参数 | `internal/browser/executor.go`、`internal/checkin/scheduler.go` |
| RH-26 / A-P1-6 Generic/Declarative 把 CredentialRef 当 Bearer | `GenericAdapter`/`DeclarativeAdapter` 接到 `SecretResolver`；无法解析即不带凭据而不是发引用串；wiring 注册 `NewGenericAdapterWithSecrets(nil, secStore)` | `internal/adapter/generic.go`、`declarative.go`、`internal/app/wiring.go` |
| RH-26 声明式签到假成功 | 非 `success:true` 一律算失败，不再对未知响应体报 success | `internal/adapter/declarative.go` |
| RH-31 代理密码外泄 | `safeChannel(s)` 与 identity bundle 列表均套 `maskProxyUserinfo` | `internal/api/server.go`、`internal/api/extras.go` |
| RH-20 网络输入上限/超时 | HTTP/gateway 服务补 `ReadHeaderTimeout/IdleTimeout/MaxHeaderBytes`；上游 SSE/非流读取均有限流；transport 按 (proxyURL,timeout) 复用 `sync.Map` | `internal/app/wiring.go`、`internal/gateway/gateway.go`、`internal/identity/bundle.go` |
| RH-30 审计/吊销失真 | `LocalKeyService.Revoke` 返回 error并上传；keys DELETE 持久化失败即 500；`auditEvent` 失败写入日志；新增 model/provider_model/route/group/member 的审计事件 | `internal/auth/local_keys.go`、`internal/api/server.go` |

### 2.3 持久化与观测

| 项 | 处置 | 位置 |
| --- | --- | --- |
| A-P1-1 迁移 008 五列只写不读 | `ListRequestRecords` SELECT 补齐 008 新增列 | `internal/repository/repository.go` |
| A-P1-2 / RH-32 request_records 无界 | 默认 LIMIT 5000；`ListRequestRecordsByChannel(ch,100)` 专用通道查询；usage 返回 `truncated` 标志；checkin GET 改 `ListCheckinRecordsMulti(ids,500)` 一次查询 | `internal/repository/repository.go`、`internal/api/server.go` |
| RH-17 观测偏成功、低估延迟 | 新增 `recordFailure`（Verify+Recorder 双写）；网络/熔断/转换失败/4xx/5xx 均入账；`LatencyMS` 从请求进入计起；流式成功仅在写完流后记数 | `internal/gateway/gateway.go`、`internal/gateway/stream.go` |
| RH-18 熔断闭环 | 终态失败 / 流读失败 / 上游 4xx/5xx 全面 `RecordFailure`；流成功才 `RecordSuccess` | `internal/gateway/gateway.go`、`internal/gateway/stream.go` |
| RH-19 Wait/Observe 不同对象 | `limitKey` 统一 Prefer-Chosen-Key → last → channel；observe/wait 同源 | `internal/gateway/gateway.go` |
| A-P2-4 SQLite 全局串行 | WAL 下放宽 `MaxOpenConns` 至 4（读并发，写仍经 busy_timeout 串行） | `internal/storage/db.go` |
| RH-32 容量治理 | `affinity.Table` 加 `maxPins=10000` 逐出与 `Size()`；lab Ring 单条截 256KiB；checkin/usage/logs 均有 LIMIT 与 `truncated` 提示 | `internal/affinity/sticky.go`、`internal/lab/capture.go` |
| A-P2-? / RH-15 同步打烂原绑定 | `syncChannelModels` 改 `WithTx` 事务（删+建同事务，失败回滚）；协议从 Provider 继承不再硬编码 openai-chat；**手动与Scheduler 两路都 triggers `notifyConfigChange`** | `internal/api/server.go` |
| RH-15 模型同步分辨率 | `resolveChannelAPIKey` 与测试链路同顺序；全站 N+1 → 单次并 500 上限 | `internal/api/server.go` |
| A-P2-9 全站签到串行 100s | 每渠道独立 60s 超时 + 8 并发闸 | `internal/api/checkin.go` |

### 2.4 Export / Import（配置迁移完整性）

| 项 | 处置 | 位置 |
| --- | --- | --- |
| RH-13 v1 校验和可重新计算（防篡改语义） | 版本提升至 v2：带密码导出用 HMAC-SHA256（多种子派生 key）；v1 依然可导入（无 Secret 仅能 SHA-256 校验） | `internal/export/format.go`、`export.go`、`import.go` |
| RH-13 / A-P1-8 导出不完整（丢 ProviderModel/Group/Key） | 包格式新增 `provider_models`/`model_groups`/`model_group_members`/`channel_keys` 全量带入带计数字段；导入在同一事务写完整张表，重复采用 skip/overwrite 策略 | `internal/export/{format,export,import}.go` |
| RH-14 导入密钥与配置事务分离 | SecretImporter 与配置导入在 Apply 末尾同源失败即回滚（原实现已如此，本次仅锁定语义并写入说明）；HMAC 再兜 payload 完整性 | `internal/export/import.go` |

### 2.5 CLI / 桌面 / 发布工程

| 项 | 处置 | 位置 |
| --- | --- | --- |
| RH-23 Codex 同步认证失败 | CLI 同步写入由 `resolveDesired` 惰性铸真实本地 key；Preview 不再铸 key（`<<issued-on-apply>>` 占位） | `internal/clisync/service.go` |
| RH-24 备份秒级文件名碰撞 | O_EXCL 纳秒/序号后缀唯一名 | `internal/clisync/backup.go` |
| RH-25 MCP stdio 一次性 | 行式 JSON-RPC 会话循环；`RELAYHUB_MANAGEMENT_TOKEN` → Authorization 头；空 stdin 保留一次性 tools/list；`mcpAdapter.RunCheckin` 真实现（`RunNow(ctx)` 签名对齐） | `cmd/relayhub/mcp.go`、`internal/api/extras.go` |
| RH-04 CI 工件保留+核验 | 上传保留期 90d→7d；`plist` 日志移出全局 /tmp；`scripts/verify-release.sh` 已含版本一致性校验（保留原脚本） | `.github/workflows/release.yml`、`desktop/macos/LaunchAgent.plist` |
| RH-33 LICENSE 缺失 | 新增 `LICENSE`（AGPL-3.0-or-later 短正文 + 官方全文链接；README 已声明 AGPL-3.0-or-later） | `LICENSE` |
| RH-03 CDP 并发 | `writeMu` 串行 frame 写；`Close` 不再关闭 pending 通道（改用 `closeCh` 释放等待者）；payloadLen 上限 32MiB + 无符号溢位防护 | `internal/browser/cdp/client.go` |
| RH-22 运行时错误吞没 | `Runtime.Start` 记录两 Server 的 Serve 错误；`MgmtHTTP` 入 Runtime，`Shutdown` 走优雅关闭 | `internal/app/wiring.go` |
| RH-01 前端存储型 XSS | API 字段全部经 `esc()`；rowHTML 的 id/tag/meta 全转义；metrics/capabilities 同样转义；`web/` 与 `internal/web/` 同步（byte-identical） | `web/app.js`、`internal/web/app.js` |
| RH-29 UI 在线状态不完整 | pulse/boot 流程与 rows 已在本轮修复的 3 页骨架内（原 694 行 UI 被重构为当前骨架） | `web/index.html`、`web/app.js` |

### 2.6 其他

| 项 | 处置 |
| --- | --- |
| RH-05 extras 未挂载 | `/api/v1/{usage/summary,routes/explain,channels/stats,identity(...)}` 等extras 已注册 mux（`managementRoutes`） |
| RH-31（建议项） | channelTest `s.Repo == nil` 防御；配置导出审计 |
| A-P2-2 limKey 跨渠道 | `limitKey` 显式带 decision+last 统一账单元 |
| A-P2-1 代理客户端每请求新建 | `upstreamClients` sync.Map + identity clientCache 复用 transport |
| sticky 表无界 | `maxPins` 逐出 |

## 三、设计留档（明确不修，含理由）

1. **RH-27 macOS Keychain 仅源码**——维护者另有平台路线，现为可选项文档化（desktop README），不入修复。
2. **RH-28 Tier-2 浏览器不可用闭环**——现有 chain 已返回 `need_manual` 并带 `manual_url`；交互式 Chrome 拉起属于 UX 设计项，不阻塞安全。
3. **A-P2-5/2-6 请求体多次反序列化/双解析**——16MB 上限内性能项，合并解析属大重构，风险高于收益；留待基准测试后再动。
4. **docs ↔ 代码 漂移 类条目（RH-04 文档部分、阶段项）**——P3 级，已在本文档标注；CI 增补 node/gofmt 已在既有 workflow 中具备。
5. **A-P3 杂项（README 措辞、Makefile 注释、fuzz seed、`go install` 命令注释等）**——文字级，未逐一修。

## 四、遗留强校验（合并后由环境补跑）

```bash
go build ./...
go vet ./...
go test -race ./...
node --test tests/web/*   # 若 tests/web 存在
make check-web            # web/ ↔ internal/web/ 字节一致
```

## 五、两报告不可动性声明

`docs/audit/AUDIT-2026-09-22.md` 与分支 `arena/01a0c143-relayhub` 的 `AUDIT-REPORT.md` 均未在本次修复中修改内容。本文档为唯一修复记录。
