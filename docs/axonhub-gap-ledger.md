# AxonHub 复刻：实现缺口账本

核对日期：2026-09-18。此文件记录实现进度，不是验收通过报告。
原需求范围仍以 `.superpowers/axonhub-replication-plan.md` 为准；原有勾选不能作为完成证据。不得因为拆分工作而取消原需求。

## 主助手已直接核对的事实

| 项目 | 状态/证据 | 后续工作 |
|---|---|---|
| 渠道表格与筛选 | `web/index.html:113-123` 仅七列，无多选；`web/app.js:235-271` 仅名称/URL搜索和启停筛选 | 增补多选及当前筛选全选，再接批量操作；其余计划列、类型/标签筛选仍待补 |
| 状态开关 | `web/app.js:253` 调用 toggleChannel，检索整个 app.js 只有调用没有定义 | 行为回归测试后实现，失败不能谎报成功 |
| 归档 | `web/app.js:589` 对 archived 仍产生 enabled=true；`internal/router/selector.go:363` 只检查 Enabled/RoutingEnabled | 先复现路由反例，再统一状态语义；不能只补角标 |
| 新建/编辑渠道保存 | `web/app.js:625-638` 多处 catch 吞失败；编辑时 body 会覆盖 credential_ref，且按名称重算 provider/channel ID | 补保存失败和保持身份/凭据引用的回归，再修保存链路 |
| 模型同步 | `internal/api/server.go:641-664` 逐条删建、忽略多处错误，无事务；协议写死 openai-chat | 子代理所称“原子重构”不成立；不得标为完整实现。需事务、错误传播、协议/手动映射保留及自动调度 |
| 连通检测 | `internal/api/server.go:701-735` 拉模型列表；延迟返回但不是实际模型推理测试；更新持久化错误被忽略 | 不把列表可访问等同模型可推理；健康历史与成功率仍待补 |

## 子代理只读发现（待对应实现前进一步复验）

### Phase B 渠道
- 完整列、类型/标签/归档筛选缺失或部分实现。
- 批量启停、删除、归档、测试、排序、标签、模板缺失。
- 健康只有简单 OK/ERR 展示，额度展示、展开行缺失。
- 多行 Key 输入拆渠道不等同单渠道多 Key 管理；增删启停与单 Key 测试未实现。
- 模型映射有表单和 ProviderModel 转发路径，但保存错误处理尚未贯通，不能整项判完成。
- 自动同步字段与手动接口存在，定时调度、正则输入缺失。
- 复制渠道缺失；归档存在上述路由风险。

### Phase C 模型
- 搜索、单模型 CRUD、tools/vision/reasoning 字段及绑定编辑存在；仍需行为回归，不宣称完整验收。
- 完整列表、多选、图标、厂商输入、模型分类、价格缺失或部分实现。
- family 不能未经需求核对就等同厂商字段。
- JSON/schema 能力、批量创建与启停、归档、聚合模型目录缺失。

### Phase D UI
- 语言切换已在设置；品牌排布部分符合。
- 多处字号小于 12pt（16px）。重大布局调整须先确认设计。

## 推进约束

1. 沿原计划分批推进，不提前转入验收。
2. 每批先 RED 行为测试，再实现，再真实验证。字段、按钮、静态检索不替代运行证据。
3. 多选只是批量操作的前置能力，完成它不等于 Phase B 或批量操作完成。
4. 涉及状态/凭据控制的修改必须独立只读审查；不得打印真实凭据。
5. Tier 2 真实运行时下载仍开放，不因本次切回复刻计划而取消。
6. 原计划范围未缩减；本轮先实现多选这一前置能力，其余仍按账本推进。

## 本轮实现：渠道多选（待独立审查和真实页面验证）

- 用户确认保持现有布局：行多选、全选当前筛选结果、半选态、已选总数；筛选变化保留选择，刷新清理已删除项，不发起批量写请求。
- 修改 `web/app.js` 渠道段与 `web/index.html` 表格，已同步至 `internal/web/`。
- 新增 `tests/web/channel-selection.test.cjs`，使用 Node 内置 test/vm 执行实际渠道脚本段；不新增应用运行时依赖。`make test-web` 已接入 `make test`（开发测试需要 Node）。
- 实测 RED：两用例失败，缺行 checkbox 与 setChannelSelected 未定义。实现后两用例通过；覆盖筛选全选/取消、隐藏选择保留、半选、刷新删除清理、空集禁用、不产生写请求。
- 实测命令链退出 0：`make sync-web && make test-web && node --check web/app.js && go test -race ./... -count=1 -timeout=200s && go vet ./... && make build && make check-web && gofmt -l cmd internal tests`。格式检查无输出。
- 测试仅覆盖脚本及 DOM 替身，不等于真实浏览器点击验证；实时页面/截图尚未检查。独立只读审查已派出，结果未回。
- 批量启停等动作仍未实现；toggleChannel 缺定义、渠道保存与归档问题未在本轮修改。未 commit/push。
